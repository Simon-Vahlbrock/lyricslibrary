import React, {FC} from 'react';
import './account.scss';

const Account: FC = () => (
    <div className="account">
        Account
    </div>
);

Account.displayName = 'Account';

export default Account
